<UserControl x:Class="Eliteria.Views.SecondView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Eliteria.Views"
             xmlns:customcontrols="clr-namespace:Eliteria.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Second" FontSize="50"/>
        <customcontrols:MenuButton Text="First" Command="{Binding NavigateFirstViewCMD}"/>
        <TextBlock Text="{Binding Username}"/>
        <TextBlock Text="{Binding Password}"/>
    </StackPanel>
</UserControl>
